use colored::Colorize;

pub fn macros() {
    println!("\n{}","||-->    So this is Macros in Rust language!...".red());
    println!("\n{}","||-->    Macros are something that have exclamation mark at the end of the method, like \"println!()\", here you can see the exclamation mark!, In simple words you can say that rust stores the metaprogamming in the macros where you don't have to write big and very complex code again and again. These operate at compile-time, which means they are expanded by the compiler before the program is run.".bright_green());
}